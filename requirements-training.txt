# Optional RazaAI model-training environment (x86_64 + NVIDIA GPU only).
# Do NOT install this on a normal Jetson/edge deployment unless you intentionally
# want a training environment.
#
# GLM-4 support (GlmForCausalLM) requires transformers >= 4.46.
# If unsloth import fails with an unsloth_zoo error, delete the
# unsloth_compiled_cache/ directory and upgrade unsloth + unsloth_zoo together.

torch
unsloth
unsloth_zoo
trl
transformers>=4.46
datasets
accelerate
bitsandbytes
sentencepiece
